// Fully static, client-rendered single-page app — no server runtime needed.
// This is what lets Sidereum run on GitHub Pages / Cloudflare Pages with zero backend.
export const prerender = true;
export const ssr = false;
export const trailingSlash = 'ignore';
